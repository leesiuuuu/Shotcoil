# 반동 슈터 (Recoil Shooter) — 게임 개발 지침서

> 이 문서 하나만 있으면 다른 세션에서 바로 이어서 개발할 수 있도록 작성됨.
> 새 세션에 이 파일을 그대로 붙여넣고 "이 스펙대로 개발 시작하자"고 하면 됨.

---

## 0. 프로젝트 개요

**한 문장 훅:** 이동 버튼이 없다. 총을 쏘면 그 반동으로 날아간다. 조준이 곧 이동이다.

이동과 공격이 하나의 입력(발사)으로 통합된 아레나 슈터. 도망치려 쏜 총알이 적을 맞추고,
적을 맞추려 쏜 총알이 나를 위험한 곳으로 밀어낸다 — 이 이중성이 게임의 깊이.

**장르:** 액션 · 아케이드 (1P)
**엔진:** Raylib (C)
**목표:** "쏘면 날아가는 손맛"이 재밌는 완성된 아케이드 게임

---

## 1. 공모전 조건 (1.44MB GAME DEV CONTEST)

출처: https://2pgarcade.com/contest-144mb.html

| 항목 | 내용 |
| --- | --- |
| 용량 제한 | 압축 해제 후 **1,474,560 바이트(1.44MB) 이하** — 실행파일·런타임 포함 |
| 실행 형태 | **독립 실행파일 필수. 웹브라우저 게임 금지** |
| 엔진 | 자유 |
| 제작 기간 | 공모전 발표(2026.05.23) 이후 신규 제작만 인정 |
| 마감 | **2026.09.04 23:39** |
| 심사기준 | ①게임 완성 여부 ②용량 준수 ③재미 (단 3개) |
| 상금 | 대상 72만 / 금상 28만 / 은상 14.4만 |
| 제출 | Google Form (https://forms.gle/BLaabf5hqMDibkcw9) |

**전략 메모**
- 용량은 생각보다 빡빡하지 않음 (사이트 예시: 619KB 쓰고 855KB 남음). Raylib 정적 링크 실행파일은 수백 KB~1MB대.
- 진짜 승부처는 **재미 + 완성도**. 심사기준이 3개뿐이라 화려함보다 "완성된 재미".
- 행사 자체가 2P(협력·경쟁) 테마 → 2P는 필수 아니지만 있으면 정체성 매치로 유리. (현재는 1P로 진행)
- 레트로·플로피 감성(1997년 하이텔 100KB 공모전 오마주) → 도트/미니멀/도형 아트가 컨셉과 잘 맞음.

---

## 2. 용량 절약 전략

- **이미지 에셋 0개.** 모든 그래픽을 도형 + 그라디언트로 그림.
  - 플레이어 = 삼각형(조준 방향 표시), 적 = 원/사각형, 총알 = 작은 원/선
- **사운드는 코드 합성음.** WAV 파일 대신 Raylib로 톤 생성 (발사/피격/폭발).
  **BGM도 마찬가지다** — 오디오 파일 0개, 실시간 칩튠 합성 (§10-3).
- **폰트는 갈무리(Galmuri11) 서브셋을 실행파일에 내장.** 아래 §12 참고 (약 48KB).
- **파티클**(반동 연기, 폭발)을 잘 뿌리면 도형 게임도 화려해 보임 — 여기에 공들일 것.
- Raylib 정적 링크(`-static`) + 스트립(`strip`) + UPX 압축으로 실행파일 최소화.

---

## 3. 코어 루프

1. 마우스로 조준 → 좌클릭 발사 + 조준 반대 방향으로 캐릭터가 밀려남(반동)
2. 반동을 이용해 공중을 날아다니며 적을 처치
3. 적 웨이브를 버티고 점수 획득 → 죽으면 즉시 재시작 → "한 번만 더"

한 판 2~3분. 죽으면 마찰 없이 바로 재시작. 최고점 갱신이 동기.

---

## 4. 조작

- **마우스 조준 + 좌클릭 발사** (반동 이동). 단일 입력이 이동·공격 겸용.
- 중력 상시 적용 → 가만있으면 추락. 계속 쏴야 떠 있음(긴장감 유지).
- 탄약 제한 + 자동 리로드(착지 시 또는 시간 경과). "무한 반동 비행" 방지가 밸런스 핵심.

---

## 5. 반동 물리 (감각의 핵심 — 여기가 게임의 전부)

- 발사 시 **조준 반대 방향으로 순간 힘(impulse)** 가함.
- 공중 관성 유지 + 약한 공기저항(air drag)으로 미끄러지는 손맛.
- 벽 튕김(bounce)은 선택 — 넣으면 콤보 플레이 깊어짐.

### 튜닝할 핵심 파라미터 5개 (이 숫자 균형이 재미를 결정)

| 파라미터 | 역할 | 시작값(가안) |
| --- | --- | --- |
| `RECOIL_IMPULSE` | 반동 세기 (한 방에 얼마나 날아가나) | 400.0 |
| `GRAVITY` | 중력 가속도 (얼마나 빨리 떨어지나) | 900.0 |
| `AIR_DRAG` | 공기저항 (미끄러지는 손맛, 1에 가까울수록 덜 감속) | 0.99 |
| `MAX_AMMO` | 최대 탄약 (연속 비행 지속 한계) | 6 |
| `RELOAD_TIME` | 리로드 시간(초) | 1.0 |
| `FIRE_COOLDOWN` | 연사 간격(초) | 0.12 |

> **가장 중요한 비율: `RECOIL_IMPULSE` ÷ `GRAVITY`** — "한 방에 얼마나 뜨는가"를 결정.
> 이 두 값부터 손으로 만져가며 잡을 것.

---

## 6. 적 / 웨이브 (재미와 리플레이성)

- **돌진형**: 플레이어에게 직진 (기본 적)
- **포탑형**: 고정, 탄막 발사 (공중 회피 강제)
- **분열형**: 처치 시 작게 쪼개짐 (반동 대혼란 유발)
- 절차적 웨이브 → 시간 지날수록 밀도·속도 상승. 코드 몇 줄로 무한 콘텐츠.
- (선택) **보스 웨이브**: 5웨이브마다 큰 적. 완성도 인상 up.

---

## 7. 스코어 & 훅

- 처치 수 + 생존 시간 + **노터치 콤보**(착지 안 하고 연속 처치 시 점수 배수)
- 최고점 저장(로컬 파일) → 자기 기록 갱신 동기. 아케이드 정통.
- 즉시 재시작 (마찰 제로).

---

## 8. 차별화 아이디어 (선택 — 나중에 붙일 것)

- **탄약이 곧 생명**: 남은 탄약 = 체력. 쏠수록 약해지고 처치로 회복 → 공격이 리스크이자 리워드.
- **슬로우모션 조준**: 조준 중 시간 느려짐(매트릭스 감각, 난이도 완화 + 연출).
- **벽 튕김 콤보**: 벽 반사를 이용한 트릭샷.

---

## 8-1. 변칙 웨이브 (Mutator) — 후반 난이도의 답

**해결한 문제:** 웨이브 25를 넘기면 계속 움직이는 변수가 **적 체력 하나뿐**이었다.
적 이동속도는 웨이브 16쯤 캡에 걸리고, 스폰 수는 12쯤 포화되고, 적 접촉 피해는
**영원히 하트 1개** 고정이었다. 반대로 플레이어는 2웨이브마다 복리로 강해지는 카드를
고르고 보스마다 HP를 +2 받는다. 그래서 체력만 올린 웨이브 35는 웨이브 20을
*더 오래 씹는* 것일 뿐 위험하지 않았다.

**설계:** 한 웨이브에 얹히는 규칙 변경. **선택이 아니라 강제**다 — 웨이브 번호가
협상 대상이 아니듯 변칙도 아니다. 돌파하면 보상이 나오지만 그건 *수락의 대가*가
아니라 *생존의 대가*다.

### 표는 의도적으로 "적 강화" 쪽으로 기울어 있다

적이 세져서 어려운 웨이브는 **어려운 웨이브**로 읽히지만, 내 총이 나빠져서 어려운
웨이브는 **게임이 조작을 뺏어간 것**으로 읽힌다. 그래서 가중치를 적 쪽에 몰았다
(적 4종 합 360 : 플레이어 6종 합 208 — 대략 5판 중 3판은 적이 바뀐다).
플레이어 쪽을 아예 안 남긴 건 아닌데, 이 게임은 *어떻게 나는가*가 전부라서
적만 강해지는 표는 **한 번도 다르게 날라고 요구하지 않기** 때문이다.

| 변칙 | 효과 | 가중치 |
| --- | --- | --- |
| **적 가속** | 적 이동 ×1.4 | 100 |
| **적 연사** | 적 공격속도 ×1.5 (모든 공격·예비동작·후딜 클럭) | 95 |
| **적 방벽** | 피격의 30%가 무효 | 85 |
| **적 흉포화** | 접촉 피해 2 (+ 무적시간 1.3→1.55초) | 80 |
| 중력 폭주 | 중력 ×1.35 | 45 |
| 반동 폭주 | 반동 ×1.75 + 공기저항 0.990→0.995 | 45 |
| 느린 탄 | 총알 속도 ×0.55 | 40 |
| 한 발의 무게 | 연사 간격 ×1.35 | 40 |
| 바닥 초토화 | 지면 인내 시간 ×0.4 | 40 |
| 능력 반감 | 공격력·탄속·탄 크기·점수 ½ | **18** |

### 등장 스케줄 (`MutSlotsFor`)

- 웨이브 **8부터**, 3웨이브에 한 번
- 웨이브 **25부터** 한 번에 **2개**
- 웨이브 **40부터** 3웨이브가 아니라 **2웨이브에 한 번**
- **보스 웨이브(5의 배수)에는 절대 안 붙는다.** 보스가 이미 그 구간의 벽이다.
- 단, 보스 웨이브에 걸린 슬롯은 **버리지 않고 다음 웨이브로 민다.** 3주기와 5주기는
  15웨이브마다 충돌하는데(20·35·50), 그냥 건너뛰면 33~37처럼 **5웨이브짜리 공백**이
  생긴다 — 하필 이 시스템이 메우려던 구간에.

실제 출현: 8, 11, 14, 17, 21, 23, 26, 29, 32, 36, 38, 41, 42, 44, 46, 48, 51, 52 …

### 보상 (돌파 시)

- **영구 공격력 +6%** (규칙 1개당 `DMG_MUTATOR = 2`스택 · 일반 클리어는 +3%)
- **새로고침 +1** (`REROLL_MAX` 이내) — 즉시 다음 카드 화면에서 쓸 수 있다는 게 핵심.
  힘든 웨이브가 *시간만 쓴 게 아니라 뭔가를 샀다*고 느끼게 하는 절반이 이쪽이다.
- 웨이브 점수 배수 `1 + 0.6 × 개수`

강화 선택 화면 하단 띠는 **다음 웨이브 예고**다(상호작용 없음). 다음이 `적 흉포화`인 걸
보고 `고화력` 대신 `활력`을 집을 수 있게 하는 것, 그게 웨이브 시작 시점이 아니라
*카드 고르기 전에* 보여주는 이유 전부다.

### 건드릴 때 반드시 지킬 것

**1. 추진력 예산.** 지속 상승 가속도는 `발사 임펄스 ÷ 발사 간격`이다.
권총 기준 `460 × 0.92 ÷ (0.32 × 0.86) ≈ 1540 px/s²`이고 중력은 900이다.
**즉 플레이어가 쓸 수 있는 상승 여력은 약 640 px/s²뿐이다.**
`중력 폭주`와 `한 발의 무게`는 **둘 다 이 예산에서 빼 간다.**
처음 잡았던 ×1.6에서는 중력 1440(여력 98) / 실질 추력 960(여력 60)이 되어
"어려운 웨이브"가 아니라 **마우스가 안 먹는 웨이브**가 됐다. ×1.35가 그 절반쯤을
남긴다. `MutThrustRule()`이 **이 둘을 절대 같이 뽑지 않는다** — 같이 걸리면 상승
여력이 음수가 되어 아예 못 뜬다.

**같은 이유로 `능력 반감`은 연사와 반동을 건드리지 않는다.** 그 둘이 곧 비행 조작이라
어느 쪽이든 반으로 줄이면 플레이어는 그냥 바닥에 붙는다. 그래서 *얼마나 자주 쏘는가*가
아니라 *쏜 게 뭘 하는가*(공격력·탄속·탄 크기·점수)를 반으로 줄인다. 이 항목의 범위를
넓히려면 반드시 위 식으로 다시 계산할 것.

**2. 명중식 무기 예외.** `느린 탄`은 레일건/레이저(hitscan)를 든 상태에서는
뽑히지 않는다(`RollMutators`). 날아가는 탄이 없어서 그 둘에겐 **어려워졌다고 말만 하고
안 어려워진 웨이브**가 되고, 플레이어가 무시하기를 학습하는 규칙은 없는 것만 못하다.

**3. `tune`을 직접 고치지 말 것.** 물리는 `MutGravity()` / `MutAirDrag()` /
`MutRecoilMul()` / `MutBulletMul()` / `MutFireMul()` / `MutGroundMul()` /
`MutContactDmg()` / `MutMercy()` / `MutHalvedMul()`를 읽는다. 전역 `tune`을 덮어썼다가
되돌리는 방식이 아니라서, 되돌리는 걸 잊어 다음 웨이브로 규칙이 새는 사고가 구조적으로
불가능하다. 수명 관리는 `StartWave`가 `mutArmed[] → mutOn[]`으로 승격하며 한 곳에서만 한다.

**4. 적 쪽 두 개는 "한 줄"에만 걸려 있다.** `적 가속`은 적 위치를 적분하는 단 한 줄
(`e->pos = ... dt * MutEnemySpeed()`), `적 연사`는 행동 스위치 안의 모든 `e->timer` /
`e->timer2` 카운트다운이 쓰는 지역 변수 `edt` 하나. **새 적 종류를 추가해도 자동으로
따라온다** — 대신 새 타이머를 `dt`로 깎으면 그 적만 `적 연사`를 무시하게 되니
`edt`를 쓸 것. `spawnT`만은 일부러 `dt`다: *몸이 실체화된다*는 예고는 언제나
온전히 줘야 한다.

**5. 실드판(`shieldMax`)은 재생·무효 대상이 아니다.** `적 방벽`은 `DamageEnemy` 안에서
굴리는데, 실드에 막힌 샷은 그 전에 `ShieldHit`으로 빠진다. "판때기는 다시 안 돌아온다"는
약속을 깨지 말 것.

---

## 8-2. 도전(퀘스트) 실패 페널티

실패하면 **그 웨이브 동안만** 공격력이 깎인다. `-10%`에서 시작해 웨이브마다 `+0.4%p`,
상한 `-30%` (`QUEST_FAIL_BASE` / `QUEST_FAIL_STEP` / `QUEST_FAIL_CAP`).
웨이브 3이면 -11%, 웨이브 38이면 -25%.

**"그 웨이브 동안만"이 설계의 전부다.** 영구 페널티였다면 `Q_NOHIT`은
*맞아서 실패 → 약해짐 → 더 맞음*의 나선이 되고, 도전은 "쫓을지 말지 고르는 것"에서
"못한 것에 매기는 세금"으로 변한다. 한 웨이브는 아플 만큼 길고, 만회할 수 있을 만큼 짧다.

만료는 `UpdateQuest`가 아니라 **`StartWave`가 `questPenalty = 0`으로** 처리한다
(웨이브가 끝났는지를 퀘스트 로직이 알 필요가 없게).
효과는 `PlayerDamageMul()` 안의 `QuestPenaltyMul()` 한 곳에만 접혀 있어서 빔·폭발·산탄이
전부 같이 약해진다. HUD의 공격력 수치는 페널티가 반영된 값이라, 태그 없이 두면
*이유 없이 숫자가 떨어진 것*으로만 보인다 — 그래서 빨갛게 변하고 `도전 실패 -25%`를 붙인다.

---

## 8-3. 방어막 · 회복 증강 쿨타임

`StackChance()`가 확률을 100%에 도달하지 못하게 막고는 있지만, 그걸로는 부족했다.
막지 못한 건 **굴리는 횟수**다. 후반 웨이브는 초당 여러 번 피격이 들어오고 넓은 무기는
초당 여러 마리를 죽이므로, 50% 무력화와 15% 회복이 *가장 희소해야 할 순간에* 상시로
터진다. 깊은 런이 죽지 않게 되는 원인이 이거였다.

쿨타임은 그 비율을 직접 고정한다 — 확률은 *다음 유효 피격을 막을지*를 정하고,
쿨타임은 *다음 유효 피격이 언제 생길 수 있는지*를 정한다.

| 증강 | 기본 대기 | 스택당 감소 | 하한 |
| --- | --- | --- | --- |
| 방어막 | 6.0초 | −0.7초 | 3.0초 |
| 흡혈 | 9.0초 | −0.9초 | 4.5초 |
| 가시 | 9.0초 | −0.9초 | 4.5초 |

스택이 **확률과 쿨타임을 동시에** 사는 구조라 두 장째도 여전히 집을 값어치가 있다.

**흡혈과 가시는 쿨타임을 공유하지 않는다.** 한 번 공유로 만들었다가 되돌렸는데,
공유하려면 카드에 "공용 대기"라고 **써야** 하고 그 문장이 창 배율 1에서 카드 폭을
넘겼기 때문이다(§10-1). 카드 두 장을 쓴 빌드가 한 장짜리보다 자주 회복하는 건 정당하다.

쿨타임은 카드 값 라인에 `무력화 24→34%  ·  4.6초` 형태로 나온다. 화살표에서 공백과
두 번째 `%`를 뺀 건 순전히 폭 때문이다 — 긴 형태는 배율 2에서 카드를 넘쳤다.

---

## 8-4. 웨이브 40+ 적 체력

`SpawnEnemy`의 체력 배수는 이제 3단이다.

```
웨이브  8~   : 선형  1 + 0.10 × (n-8)
웨이브 16~   : × 1.035^(n-16)    (복리)
웨이브 40~   : × 1.055^(n-40)    (복리, 추가)
```

40 이후가 필요한 이유: 그 시점이면 플레이어는 스무 장 넘는 강화를 집었고(전부 곱연산)
거기에 **매 웨이브 클리어마다 붙는 영구 공격력**이 더해진다. 3.5% 램프는 30대 어딘가에서
그 성장을 따라가지 못한다. 5.5%를 얹으면 웨이브 50의 잡몹이 웨이브 8 기준 약 **27배**가
되어(기존 17배) 40대가 다시 등반 구간이 된다.

보스는 여기 해당 없음 — 보스는 `BOSS_LAP_GROWTH`로 **랩(5웨이브)마다** 따로 복리를 먹는다.

---

## 9. 개발 로드맵 (한 달)

### ★ STEP 1 (프로토타입, 최우선) — "반동 손맛" 검증
목표: 적도 스코어도 없이 **쏘면 날아가는 감각이 재밌는가**만 확인.
- [ ] 플레이어: 조준 방향 표시되는 삼각형
- [ ] 중력 상시 적용 + 바닥 충돌
- [ ] 좌클릭 → 조준 반대로 impulse + 총알 발사(비주얼)
- [ ] 관성 + 공기저항(미끄러지는 손맛)
- [ ] 반동 연기 파티클
- [ ] 탄약 제한 + 착지 시 리로드
- [x] ~~파라미터 5개를 화면에서 실시간 조절 가능하게~~ (F1 튜닝 패널 — 손맛 확정 후 제출본에서 제거함)

> 이게 재밌으면 게임 전체가 재밌다. 여기서 파라미터 확정하고 다음으로.

### STEP 2 (2주차) — 적 & 웨이브
- [ ] 돌진형 적 + 충돌 판정(총알↔적, 적↔플레이어)
- [ ] 절차적 웨이브 스폰
- [ ] 포탑형/분열형 적 추가
- [ ] 플레이어 사망 → 재시작

### STEP 3 (3주차) — 스코어 & 연출 & 사운드
- [ ] 스코어(처치+생존+콤보), 최고점 저장
- [ ] 폭발/피격 파티클, 화면 흔들림(screen shake)
- [ ] 코드 합성 사운드(발사/피격/폭발)
- [ ] 타이틀/게임오버 화면

### STEP 4 (4주차) — 밸런싱 & 폴리싱 & 제출 준비
- [ ] 난이도 곡선 튜닝
- [ ] 정적 링크 + strip + UPX로 용량 최적화 → 1.44MB 확인
- [ ] Windows 실행파일 빌드 & 테스트
- [ ] Google Form 제출

---

## 10. 프로젝트 세팅 (Raylib / C)

### 실제 구조 (현재)
```
Shotcoil/
├── Project1/
│   ├── Project1.sln
│   └── Project1/
│       ├── main.c
│       └── Project1.vcxproj
├── third_party/
│   ├── build-raylib.ps1     # raylib 정적 빌드 스크립트
│   ├── raylib/              # 산출물 (커밋됨) — include/, lib/raylib.lib
│   ├── raylib-6.0/          # 원본 소스 (gitignore)
│   └── raylib-build/        # CMake 빌드 트리 (gitignore)
├── packaging/
│   ├── README.txt           # 심사자용 안내문 (제출물에 동봉)
│   └── make-dist.ps1        # 빌드 → 수집 → 용량 검사 → zip
└── CLAUDE.md
```

폰트 관련 파일:
```
Project1/Project1/galmuri.h  # 생성물 (커밋됨) — 서브셋 TTF 바이트 배열
third_party/make-font.py     # galmuri.h 생성기
third_party/fonts/           # 원본 Galmuri11.ttf / Galmuri11-Bold.ttf / OFL.txt
```

빌드: Visual Studio 2022, **x64 전용** (Win32 구성 없음).
출력: `Project1/x64/Release/Shotcoil.exe` — DLL 0개.
프로젝트/폴더 이름은 `Project1`이지만 산출물 이름은 vcxproj의
`<TargetName>Shotcoil</TargetName>`으로 분리해 둔다 (제출물이 `Project1.exe`면
곤란하므로 **지우지 말 것**).

현재 **1,267,200 바이트** (제출 폴더 합계 1,274,797 — 제한 1,474,560, 여유 약 195KB).
이 중 내장 폰트가 약 69KB, 아이콘 리소스가 약 6KB. BGM은 실행파일을 **약 6KB**
늘렸다 (§10-3).

제출 패키지는 `packaging/make-dist.ps1`이 만든다. Release를 리빌드하고
실행파일·안내문·폰트 라이선스를 `dist/Shotcoil/`에 모아 **압축 해제 기준**
합계를 1,474,560과 대조한 뒤 `dist/Shotcoil.zip`으로 압축한다. 규정이 재는
값이 압축 후가 아니라 해제 후 크기라서 zip 크기가 아닌 폴더 합계를 검사한다.
zip 안에는 `Shotcoil\` 폴더가 하나 들어 있다 — 내용물만 담으면 압축 푸는 도구에
따라 파일 세 개가 그대로 쏟아진다.

### Release는 Windows 서브시스템, Debug는 Console

Release|x64만 `<SubSystem>Windows</SubSystem>` + `<EntryPointSymbol>mainCRTStartup</EntryPointSymbol>`이다
(진입점은 그대로 `main()`이라 WinMain은 필요 없다). Console로 두면 exe를 더블클릭할 때
**게임 창 뒤로 검은 콘솔이 같이 뜨고 raylib 로그가 흐른다** — 심사자가 처음 보는 화면이
그것이면 곤란하다. Debug는 Console 그대로여야 한다: `--shot` / `--music` / `--list`가
stdout으로 답한다.

Release는 `<GenerateDebugInformation>false</GenerateDebugInformation>`이기도 하다.
켜두면 PDB 절대경로(개발 PC의 폴더 구조)가 실행파일에 박힌다.

`third_party/fonts/`의 원본 TTF와 `make-font.py`는 빌드 시점 도구일 뿐
실행파일에는 들어가지 않는다 (서브셋 결과인 `galmuri.h`만 컴파일됨).

### ⚠️ raylib는 vcpkg를 쓰지 말 것 (중요)

**증상:** 창이 뜨고 콘솔 로그도 정상인데 **화면이 흰색 그대로 멈춤.**

**원인:** vcpkg의 `ports/raylib/portfile.cmake`가 `-DCUSTOMIZE_BUILD=ON`을 넘긴다.
그러면 raylib의 `ParseConfigHeader`가 `src/config.h` 항목들로 CMake 옵션을 자동
생성하는데, 이때 `SUPPORT_CUSTOM_FRAME_CONTROL`이 **기본 ON**으로 잡힌다
(config.h 원본 값은 `0`). 이 플래그가 켜지면 `EndDrawing()`이
`SwapScreenBuffer()` / `PollInputEvents()` / FPS 제한을 **전혀 수행하지 않는다.**
→ 백버퍼에는 그려지지만 화면에 present가 안 되어 흰 화면.

**진단법:** `SetTargetFPS(60)` 걸고 60프레임 도는 시간 측정.
정상이면 ~1.0초, 버그면 ~0.000초.

**해결:** `third_party/build-raylib.ps1`로 `CUSTOMIZE_BUILD=OFF` + 정적 빌드.
config.h가 그대로 적용되고, DLL도 사라져 공모전 독립 실행파일 조건까지 해결됨.

vcxproj에는 `<VcpkgEnabled>false</VcpkgEnabled>`가 설정되어 있다 —
**절대 지우지 말 것.** 지우면 vcpkg가 버그 있는 raylib를 다시 주입한다.

링크 라이브러리: `raylib.lib opengl32.lib gdi32.lib winmm.lib shell32.lib user32.lib`
런타임: **Debug/Release 모두 `/MT`** (raylib.lib이 /MT로 빌드됨 — 맞추지 않으면 링크 충돌).

### ⚠️ `/utf-8` 컴파일 옵션을 지우지 말 것

`main.c`에는 한글 문자열 리터럴이 들어 있다. BOM 덕분에 MSVC가 **소스**는 UTF-8로
읽지만, **실행 문자 집합**은 기본적으로 시스템 코드페이지(한국어 Windows면 CP949)다.
즉 `/utf-8` 없이 빌드하면 실행파일 안의 문자열이 CP949 바이트로 들어가고,
raylib는 이를 UTF-8로 해석하므로 한글이 전부 깨진다.

vcxproj의 Debug/Release x64 양쪽 `<AdditionalOptions>/utf-8 ...`가 그 방어선이다.

### 빌드 예시 (Linux/Mac, 정적 링크)
```bash
gcc src/main.c -o build/game \
    -lraylib -lm -lpthread -ldl -lrt -lX11 \
    -O2 -static
strip build/game        # 심볼 제거로 용량 감소
upx --best build/game   # (선택) 추가 압축
```

### Windows 크로스빌드/네이티브 빌드 시
- Raylib를 정적 라이브러리(libraylib.a)로 링크.
- 최종 제출은 Windows `.exe` 권장(심사 환경 고려).
- MinGW-w64 사용 예: `gcc src/main.c -o game.exe -lraylib -lopengl32 -lgdi32 -lwinmm -O2 -static`

---

## 10-1. 한글 폰트 (갈무리 Galmuri11)

UI 텍스트는 전부 한글이고, **Galmuri11 Regular + Bold** 두 굵기를 쓴다.
(굵기가 두 종류인 갈무리는 11px판뿐이다 — 9/14px판은 Regular만 있음.)

### 픽셀 격자: 폰트 크기는 반드시 **14의 배수**

Galmuri11은 upem 1200에 **100단위 = 디자인 1픽셀** 격자로 그려져 있다.
raylib는 `stbtt_ScaleForPixelHeight`로 폰트를 재는데, 이 폰트의
`ascent - descent`는 1400이다. 따라서

```
디자인 1픽셀 = 100 * (fontSize / 1400) 화면픽셀
```

fontSize가 **14의 배수**일 때만 이 값이 정수가 된다. 그 외 크기에서는
글자 획 경계가 픽셀 중간에 걸려 굵기가 들쭉날쭉해진다 — 픽셀 폰트를
싸구려로 보이게 만드는 유일한 원인.

게다가 게임은 1280x720 가상 좌표를 창 크기에 맞춰 확대(`viewScale`)하므로
"가상 단위로 14의 배수"만으로는 부족하다 (1.5배 레터박스면 21 실제픽셀).
그래서 `main.c`의 `UISize()` / `UISnap()`이 **실제 화면 픽셀 기준**으로
크기와 좌표를 격자에 맞춘 뒤 가상 단위로 되돌린다.

텍스트를 그릴 때는 `DrawText` 대신 이 헬퍼들을 쓸 것:

| 함수 | 용도 |
| --- | --- |
| `UIDraw(FW_REG\|FW_BOLD, txt, x, y, size, color)` | 좌측 정렬 |
| `UIDrawC(...)` | `x`를 중심으로 가운데 정렬 |
| `UIWidth(...)` / `UIMeasure(...)` | 폭 측정 (그리기와 동일한 스냅 적용) |

`MeasureText`로 재고 `UIDraw`로 그리면 정렬이 어긋난다 — 반드시 짝을 맞출 것.
글리프 아틀라스는 `FONT_BITMAP`(안티에일리어싱 없음) + `TEXTURE_FILTER_POINT`다.

### 텍스트를 추가/수정하면 폰트를 다시 만들 것

`galmuri.h`에는 **`main.c`에 실제로 등장하는 글자만** 들어 있다 (현재 239자,
Regular 26KB + Bold 21KB). 새 한글을 쓰면 그 글자는 두부(`?`)로 나온다.

```bash
python third_party/make-font.py     # main.c를 훑어 galmuri.h를 다시 생성
```

fontTools가 필요하다: `pip install fonttools`.

폰트 라이선스는 SIL OFL 1.1 (`third_party/fonts/OFL.txt`).
**배포·제출 시 이 라이선스 고지를 함께 포함해야 한다.**

---

## 10-2. 캡처 모드 (`--shot`) — UI 변경 검증용 · **Debug 빌드 전용**

이 게임은 **바깥에서 자동화할 수 없다.** 전체화면에서 `DisableCursor`로 포인터를
잡아버려서 SendKeys / mouse_event로 주입한 입력이 창에 도달하지 않는다. 게다가
확인하고 싶은 화면 대부분(웨이브 12의 도전 HUD, 강화 선택 카드, 증강 범위 링)은
타이틀에서 2분씩 플레이해야 나온다.

그래서 화면 하나를 세팅하고 → 정해진 시간만큼 시뮬레이션하고 → PNG로 저장하고
종료하는 모드를 넣어 두었다. 단, **제출용 Release에는 들어가지 않는다.**

### 빌드 분리

캡처 모드 전체가 `#ifdef SHOTCOIL_CAPTURE` 안에 있고, 이 매크로는
**Debug|x64 구성에서만** 정의된다 (`Project1.vcxproj`의 `PreprocessorDefinitions`).
심사 기준은 ①완성 ②용량 ③재미뿐이고, 심사자가 요구하지 않은 개발자 플래그가
제출 바이너리에 남을 이유가 없다. Release에는 `argv` 처리조차 없다
(`main(void)`로 컴파일된다).

```powershell
# 캡처용 (도구)
msbuild Project1\Project1.sln /p:Configuration=Debug /p:Platform=x64
Project1\x64\Debug\Shotcoil.exe --shot=help --scale=2

# 제출용 (캡처 코드 0바이트)
msbuild Project1\Project1.sln /p:Configuration=Release /p:Platform=x64
```

확인법: Release 바이너리를 문자열 검색해서 `--shot=SCENE`이 안 나오면 성공.
두 구성 모두 `/MT` + `/utf-8` + raylib 정적 링크라 렌더 결과는 동일하다.

### 사용법

```bash
Shotcoil.exe --shot=SCENE [옵션]      # Debug 빌드

  --shot=SCENE   title | help | play | upgrade | gameover
  --out=FILE     저장 경로 (상대=exe 옆, 절대경로도 가능)
  --scale=N      창 배율 1..4           (기본 1)
  --wave=N       세팅할 웨이브          (기본 1)
  --at=SEC       캡처 전 시뮬레이션 시간 (기본 1.0)
  --seed=N       난수 시드              (기본 1)
  --weapon=KEY   pistol smg sword shotgun railgun grenade bazooka ...
  --aug=K,K,...  증강 1스택씩 부여 (같은 키를 반복하면 중첩)
  --cards=K,K,K  제시되는 강화 카드 3장을 고정 (upgrade 전용)
  --mut=K,K      변칙 — upgrade 씬에서는 예고, play 씬에서는 적용 규칙
                 swift barrage ward savage
                 gravity recoil slowbullet heavyshot scorch halved
  --quest=STATE  active | fail | done   (play 전용)
  --qkind=KIND   airkill | nohit | speedkill | longshot  (도전 종류 고정)
  --fire         캡처 내내 발사 버튼을 누르고 있음
  --aim=X,Y      조준점 (1280x720 단위, 기본 640,300)
  --music=TRACK  title | battle — 곡을 wav로 렌더링하고 종료
                 (--at 로 길이, --out 으로 파일명. 창을 열지 않는다)
  --list         무기/증강 키 목록 출력 후 종료
```

예:

```bash
Project1\x64\Debug\Shotcoil.exe --shot=upgrade --wave=5   # 보스 클리어 강화 화면
Shotcoil.exe --shot=play --qkind=longshot                 # 먼 거리 도전 + 범위 원
Shotcoil.exe --shot=play --quest=fail --at=2.5            # 도전 실패 UI가 사라졌는지
Shotcoil.exe --shot=play --aug=homing,sniper --fire       # 발사 연동 범위 링
Shotcoil.exe --shot=help --scale=2                        # 폰트 격자 다른 칸에서 레이아웃
Shotcoil.exe --shot=upgrade --wave=26 --mut=barrage,halved             # 변칙 2개 예고 띠
Shotcoil.exe --shot=play --wave=26 --mut=swift,ward --at=0.9           # 변칙 웨이브 시작 배너
Shotcoil.exe --shot=upgrade --cards=aegis,lifesteal,thorns --scale=1   # 쿨타임 값 라인 폭
Shotcoil.exe --shot=play --wave=38 --quest=fail --qkind=nohit          # 도전 실패 페널티 HUD
```

**`--scale`을 반드시 바꿔가며 볼 것.** §10-1대로 폰트 크기가 실제 14픽셀 격자에
스냅되므로, 배율이 다르면 같은 문장이 가상 단위로 1.5배까지 넓어진다. 한 배율에서
패널에 들어가던 줄이 다른 배율에서 넘칠 수 있고, 이게 이 UI가 깨지는 가장 흔한 방식이다.

구현 메모:
- 프레임은 고정 1/60초로 돌린다. 같은 명령은 항상 같은 그림을 낸다.
- 캡처 모드는 **창 모드 + 커서 자유**다. 레터박스 없는 정확한 `1280*scale x 720*scale`
  PNG가 나오고, 실패해도 커서가 갇히지 않는다.
- 조준점도 고정한다(`shotAim`). 실제 마우스를 읽으면 실행할 때마다 그림이 달라진다.
- 화면 세팅은 게임이 실제로 쓰는 `ResetGame` / `StartWave` / `RollUpgrades`를 그대로
  호출한다 — 목업이 아니라 진짜 화면이다.
- Debug 빌드에서도 `--shot` 없이 실행하면 평소처럼 전체화면으로 게임이 시작된다.
  `shotOn`/`shotFire`가 false로 남고 `UpdateAimCursor`/`UpdatePlayer`의 훅은 통과한다.
- `--cards`가 있는 이유: 카드 롤은 가중치 추첨이라 희귀한 증강 하나가 뜨는 데
  시드를 열 몇 개씩 돌려야 한다. 그러면 "값 라인이 카드에 들어가는가"가 검증이
  아니라 운이 된다.
- `SHOT_WEAPON_KEYS` / `SHOT_AUG_KEYS` / `SHOT_MUT_KEYS`는 C 정적 어서션
  (`typedef char x[cond?1:-1]`)으로 `WP_COUNT` / `UP_COUNT` / `MUT_COUNT`와 길이를
  검사한다. 표에 항목을 추가하고 키를 안 넣으면 **컴파일 에러**가 난다 —
  조용히 엉뚱한 증강이 부여되는 대신.

---

## 10-3. 음악 (BGM) — 실시간 칩튠 스트리밍

**오디오 파일은 용량이 허락하지 않는다.** 남은 여유가 약 195KB인데, 얇은 비트레이트의
30초 모노 OGG조차 그걸 통째로 먹는다. 그래서 효과음과 같은 방식으로 **합성**한다.
다만 곡은 Wave 하나로 들고 있기엔 너무 길어서, `LoadAudioStream`에 **한 청크씩
렌더링해 넣는다** (22050Hz · 16bit · 모노 · 1024프레임 = 46ms).

실행파일 증가분은 **약 6KB**. 패턴 데이터는 전부 합쳐 800바이트 남짓이다.

### 음색은 NES 그대로 4성부

펄스 2개 + 삼각파 베이스 + 노이즈 드럼. **4성부는 우회해야 할 제약이 아니라 그 소리
자체다.** 삼각파를 16단계로 양자화(`MusTriangle`)하는 것도 같은 이유 — 그 미세한
버즈가 없으면 그냥 사인파 베이스로 들린다.

| 곡 | BPM | 편성 | 쓰이는 곳 |
| --- | --- | --- | --- |
| `MUS_TITLE` | 100 | 리드·화음·베이스 (**드럼 없음**) | 타이틀 · 게임 방법 · 게임오버 |
| `MUS_BATTLE` | 150 | + 킥/스네어/하이햇 | 플레이 · 강화 선택 |

타이틀에 드럼이 없는 건 실수가 아니다. 반응을 요구하지 않는 화면이라 느리고 성기게 둔다.

### 패턴 인코딩 — `0`과 `1`을 절대 섞지 말 것

```
0        = 아무것도 하지 않음 (앞 음을 유지)
1        = 음 끊기
그 외    = MIDI 노트 번호
```

**쉼표를 `0`이 아니라 `1`로 명시하는 게 핵심이다.** "유지"와 "무음"이 같은 바이트면,
손으로 친 패턴에서 *음이 빠진 것*이 *음이 틀린 것*처럼 들리고 원인을 못 찾는다.

드럼만 128칸이 아니라 **한 마디(16칸)**만 저장하고 반복한다. 4·8마디째에는
`BATTLE_DRUMFILL`이 들어가는데, 그 두 자리가 루프가 루프로 들리기 시작하는 지점이다.

### 건드릴 때 지킬 것

**1. `MusRender`는 오디오 스레드 쪽 코드다.** 곡 전환(`musTrack != musWant`)도 그
안에서 샘플 단위로 크로스페이드한다 — 게임 스레드에서 바꾸면 음 중간을 잘라먹는다.
게임 쪽이 하는 일은 `musWant`를 정하는 `MusicForState()` 하나뿐이다.

**2. 매 프레임 `UpdateMusic()`을 불러야 한다.** 프레임이 길게 튀면 버퍼가 마르고
소리가 끊긴다. 청크 1024 × 버퍼 2개 = 약 93ms의 여유가 방어선이다.

**3. 음량은 효과음 아래.** 스트림 볼륨 0.45 + 내부 믹스 계수(0.20/0.13/0.26/0.22).
플레이어가 실제로 듣고 반응하는 피드백은 **발사음**이지 BGM이 아니다.

**4. `M` 키로 끈다.** 설정 화면이 없으므로 어느 화면에서나 먹고, 상태(`켜짐`/`꺼짐`)는
타이틀 하단 안내줄에 나온다.

### 들어보고 고치기

곡을 고치면 게임을 켜지 말고 wav로 뽑아서 확인한다 (Debug 빌드):

```bash
Project1\x64\Debug\Shotcoil.exe --music=title  --at=20 --out=title.wav
Project1\x64\Debug\Shotcoil.exe --music=battle --at=14 --out=battle.wav
```

`ShotWriteMusic`은 **게임이 쓰는 `MusRender`를 그대로** 호출한다 — wav에 담기는 건
게임이 내는 소리 그 자체이지, 옆에서 따로 굴러가다 어긋날 수 있는 두 번째 구현이
아니다. 오디오 장치도 창도 열지 않는다.

한 바퀴 길이: 타이틀 19.2초, 전투 12.8초 (둘 다 128스텝).

---

## 11. 다음 액션

**STEP 1 프로토타입 코드부터 시작.**
새 세션에서 이 문서를 붙여넣고 다음처럼 요청:

> "이 스펙의 STEP 1 프로토타입을 Raylib(C)로 작성해줘.
> 플레이어 삼각형 + 중력 + 반동 발사 + 파티클 + 파라미터 실시간 조절까지.
> 파라미터 5개는 키보드로 실시간 증감 가능하게."

그 다음 손맛 튜닝 → STEP 2로 진행.

---

## 부록: 확정된 결정사항 요약

- 장르: 1P 액션·아케이드 반동 슈터
- 엔진: **Raylib (C)** — 개발자 C/C++ 익숙
- 아트: 도형 + 그라디언트, 이미지 에셋 0개
- 사운드: 코드 합성음 — 효과음 8종 + **BGM 2곡(칩튠 스트리밍, §10-3)**, 오디오 파일 0개
- 최우선 작업: **반동 물리 손맛 프로토타입 (STEP 1)**
- 가장 중요한 튜닝 값: `RECOIL_IMPULSE` ÷ `GRAVITY` 비율
- 후반 난이도는 **강제 변칙 웨이브**로 해결 (§8-1). 표는 적 강화 쪽으로 기울여 둠 —
  적이 세져서 어려운 건 어려운 웨이브지만 내 총이 나빠져서 어려운 건 조작을
  뺏긴 것으로 읽힌다.
- 도전 실패 페널티는 **한 웨이브 한정** (§8-2). 영구로 만들면 `Q_NOHIT`이
  맞아서 실패 → 더 아파짐의 나선이 된다 — 기간 제한이 그 방어선이다.
- 무력화/회복 증강은 확률이 아니라 **쿨타임**으로 통제 (§8-3). 확률 상한만으로는
  후반의 굴림 *횟수*를 못 막는다.
- BGM은 **스트리밍 칩튠** (§10-3). 오디오 파일은 남은 195KB로 30초도 못 사는데,
  합성은 실행파일 6KB로 두 곡을 산다. 곡 수정은 `--music`으로 wav를 뽑아 확인할 것.